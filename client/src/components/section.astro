---
interface Props {
  title: string;
  class?: string;
}
const { title, class: className } = Astro.props;
---

<section
  id={title}
  class=`box-content px-5  mx-auto max-w-5xl md:py-10 lg:max-w-6xl ${className}`
>
  <slot />
</section>
